/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'your-supabase-project.supabase.co',
      // Add your actual Supabase project domain here
    ],
  },
}

module.exports = nextConfig